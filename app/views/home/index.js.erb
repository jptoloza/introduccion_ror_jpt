$('#images').append('<%= j render partial: 'home/pin', collection: @pins %>');



console.log('<%= @pins.next_page%>')

<% if @pins.next_page %>
	$('.pagination').replaceWith('<%= j will_paginate @pins%>');	
<% else %>
	$('.pagination').remove();
<% end %>